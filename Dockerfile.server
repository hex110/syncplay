FROM python:3.12-slim

WORKDIR /app

# Install only server dependencies (no GUI needed)
RUN pip install --no-cache-dir twisted[tls] certifi pem

COPY syncplay/ ./syncplay/
COPY syncplayServer.py .

EXPOSE 8999

ENTRYPOINT ["python", "syncplayServer.py"]
